# Tech Debt Tracker

这个文件记录不值得立刻阻塞当前工作、但值得持续回收的 repo-level debt。

## Current Items

- 为 repo workspace lint 增加更细的 cross-link freshness 检查
- 给 `web/` 与 `plugins/obsidian-audit/` 建立更系统的 test strategy 摘要
- 逐步把 repo-level 质量规则从文档迁移到可执行检查
- 评估未来是否需要 GitHub Actions 版本的 workspace lint
